{
  "name": "daily-note-flow",
  "version": "0.3.0",
  "description": "DailyNote as an Obsidian community plugin with pure Markdown storage.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
  },
  "keywords": [
    "obsidian",
    "daily-note",
    "markdown",
    "productivity"
  ],
  "author": "zhimahuzzz",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.0.0",
    "esbuild": "^0.25.0",
    "obsidian": "^1.8.7",
    "typescript": "^5.8.0"
  }
}
